/* public class Test extends Terminal {

    public static void main(String[] args) {

        Circle circle = new Circle();
        circle.setX(20);
        circle.setRadius(15);

        Rectangle square = new Rectangle();
        square.setX(18);
        square.setWidth(10);

        printLine("width = %d", square.getWidth());
        printLine("height = %d", square.getHeight());

        square.setHeight(20);

        printLine("width = %d", square.getWidth());
        printLine("height = %d", square.getHeight());

        Console.writeLine(circle.getX());
        Console.writeLine(square.getX());

        Square s = null;
        s = new Square();
        s.setWidth(10);
        s.setHeight(20);
        printLine(" El area es %f", s.getArea());

        Shape[] shapes = new Shape[6];
        float areaTotal = 0;
        shapes[0] = new Circle();
        shapes[1] = new Elipse();
        shapes[2] = new Triangle();
        shapes[3] = new Square();
        shapes[4] = new Triangle();
        shapes[5] = new Rectangle();

        ((Circle) shapes[0]).setRadius(3);

        for (var suma : shapes)
            areaTotal += suma.getArea();

    }
}
*/



